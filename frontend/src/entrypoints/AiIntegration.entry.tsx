import React from "react";
import ReactDOM from "react-dom/client";
import AiIntegration from "../AiIntegration";
import "../index.css";

ReactDOM.createRoot(document.getElementById("ai-integration-root")!).render(
  <React.StrictMode>
    <AiIntegration />
  </React.StrictMode>
);