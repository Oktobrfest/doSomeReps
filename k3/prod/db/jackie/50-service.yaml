apiVersion: v1
kind: Service
metadata:
  name: postgresql-jackie
  namespace: reps-prod
spec:
  selector:
    app: postgresql-jackie
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
  type: ClusterIP